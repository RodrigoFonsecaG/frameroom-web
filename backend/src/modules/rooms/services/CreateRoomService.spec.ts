import CreateRoomService from './CreateRoomService';

describe('CreateRoom', () => {
    it('should be able to create a new room', () => {
        
    })

    // it('should not be able to create two rooms with the same primary code', () => {

    // })
})
